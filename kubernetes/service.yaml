apiVersion: v1
kind: Service
metadata:
  name: node-api
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: node-api
  ports:
    - name: api-web
      port: 80
      targetPort: api-web
      protocol: TCP
